{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "projectName": { "value": "bankproj" },
    "sqlAdminLogin": { "value": "${{ secrets.SQL_ADMIN_USERNAME }}" },
    "sqlAdminPassword": { "value": "${{ secrets.SQL_ADMIN_PASSWORD }}" },
    "debug": {"value" : true}
  }
}
